<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item android:id="@+id/search"
        android:title="@string/movie_search"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="ifRoom|collapseActionView"
        app:actionViewClass="android.support.v7.widget.SearchView" />


    <item android:title="@string/search_selection_top_menu"
        android:orderInCategory="99" >
        <menu>
            <group android:checkableBehavior="single" android:visible="true"   >
                <item
                    android:id="@+id/search_character"
                    android:orderInCategory="100"
                    app:showAsAction="collapseActionView"
                    android:title="@string/search_character" />
                <item
                    android:id="@+id/search_word"
                    android:orderInCategory="100"
                    app:showAsAction="collapseActionView"
                    android:title="@string/search_word"/>
                <item
                    android:id="@+id/search_buffer"
                    android:orderInCategory="100"
                    app:showAsAction="collapseActionView"
                    android:title="@string/search_buffer"/>
                <item
                    android:id="@+id/search_phrase"
                    android:orderInCategory="100"
                    app:showAsAction="collapseActionView"
                    android:title="@string/search_movie_title"/>

            </group>

        </menu>

    </item>

</menu>
